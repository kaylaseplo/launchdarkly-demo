import { LDLogger } from '../../api';
import { DataSourceErrorKind } from '../../datasource';
import { FDv2Event } from './proto';
import { ObjProcessors, PayloadListener } from './protocolHandler';
/**
 * Parses payloads from a stream of FDv2 events by delegating to a protocol handler.
 * Sends completed payloads to registered listeners.
 * Invalid event sequences may be dropped silently, but the processor will continue to operate.
 */
export declare class PayloadProcessor {
    private readonly _errorHandler?;
    private readonly _logger?;
    private _listeners;
    private readonly _handler;
    constructor(objProcessors: ObjProcessors, _errorHandler?: ((errorKind: DataSourceErrorKind, message: string) => void) | undefined, _logger?: LDLogger | undefined);
    addPayloadListener(listener: PayloadListener): void;
    removePayloadListener(listener: PayloadListener): void;
    processEvents(events: FDv2Event[]): void;
}
//# sourceMappingURL=payloadProcessor.d.ts.map